# 사장님앱 통계 메뉴 프로토타입 작업계획서

> 작성일: 2026-07-13
> 작성 목적: 데이터서비스팀 · 서비스개발팀과의 "사장님앱 통계 메뉴 구현 데이터 파이프라인 구조 설계" 미팅 준비
> 참고 문서: `사장님앱 통계 데이터 구성안 (v2)` (지표 요건 원본)

---

## 1. 배경 및 목적

- 사장님앱 고도화 우선순위 항목 중 하나인 "해피포인트 멤버십 데이터 기반 통계 제공"을 구현하기 위해, **오더/HPC/DW 3개 시스템 담당팀 간 데이터 파이프라인 구조 설계 미팅**을 진행한다.
- 미팅에 앞서 기획/서비스 측에서 **"어떤 통계 메뉴를 어떤 형태로 보여줄 것인가(요건)"**를 먼저 확정하고, 이를 시각적으로 검증 가능한 **클릭 프로토타입**으로 만들어 미팅 자료로 활용한다.
- 본 문서 범위는 **요건 정리 + 프로토타입 제작 계획**이며, 데이터 처리 주체(오더 API vs DW 배치)·실제 집계 로직 구현은 미팅 이후 논의 대상으로 범위 밖이다.

---

## 2. 프로토타입 목표 정의

| 항목 | 내용 |
|---|---|
| 화면 수 | 총 **6개 화면** (메뉴 2개 × 탭/그룹 3개) |
| 메뉴 1 (Track 1) | 실시간 매출 통계 — 오늘 / 이번 주 / 이번 달 (3탭) |
| 메뉴 2 (Track 2) | 고객 심층 분석 — 누가 오는가 / 왜·언제 오는가 / 돈이 되는가 (3그룹, 탭 없이 스크롤) |
| 데이터 | 전부 **더미데이터**로 구성된 차트 (실 데이터 연동 없음) |
| 인터랙션 | 각 차트를 **호버(또는 탭)하면 해당 지표를 설명하는 JSON 데이터가 팝업으로 노출** |
| 목적 | 실제 코드 구현이 아닌, **미팅에서 "이런 화면을 위해 이런 데이터가 필요하다"를 설명하기 위한 시각 자료** |

### 산출물이 아닌 것 (Out of Scope)
- 실 데이터 연동 / API 설계
- 데이터 집계 주체(오더 vs DW) 결정
- 세그먼트 판정 배치 로직 구현
- 쿠폰 연결 등 후속 기능 구현

---

## 3. 화면별 구성 (6 Screens)

### 3-1. Track 1 — 실시간 매출 통계 (오더 거래원장, 3탭)

| 탭 | 카드/차트 구성 순서 | 카드 수 |
|---|---|---|
| ① 오늘 | KPI(2x2) → 시간대별 → 인기메뉴Top3 → 배달/픽업 비중 → 채널별 매출 | 5 |
| ② 이번 주 | 요일별 누적(최상단) → KPI(2x2) → 시간대별 → 인기메뉴Top3 → 배달/픽업 비중 → 채널별 매출 | 6 |
| ③ 이번 달 | KPI(2x2) → 시간대별 → 요일별 평균 → 인기메뉴Top3 → 배달/픽업 비중 → 채널별 매출 | 6 |

- KPI 카드 4개(매출액/건수/객단가/취소율)는 탭마다 대비 기준만 달라짐(전일/전주/전월).
- **요일별 누적(이번 주)**: 미래 요일은 막대 미표시, 오늘은 강조색 — 더미데이터에도 동일 규칙 반영 필요.
- **요일별 평균(이번 달)**: 전 요일 표시, "합계 ÷ 지나간 횟수" 로직을 더미데이터 생성 시에도 반영.

### 3-2. Track 2 — 고객 심층 분석 (HPC 멤버십, 3그룹·탭 없음)

| 그룹 | 카드 구성 순서 | 카드 수 |
|---|---|---|
| ① 누가 오는가 | 고객 구성(단골비중 헤드라인+세그먼트 칩4) → 단골·신규 추이(6개월) → 세그먼트 타겟 쿠폰 연결 CTA | 3 |
| ② 왜·언제 오는가 | 선호 상품·카테고리 → 방문 시간대 → 재방문 주기 | 3 |
| ③ 돈이 되는가 | 포인트 활용지표(H값) → 세그먼트별 매출 기여도(표) → G-CRM 인근 가맹점 비교 | 3 |

- 그룹1의 "세그먼트 타겟 쿠폰 연결" 카드는 오픈이슈 항목이므로, 프로토타입에서는 **비활성/안내 문구 형태**의 CTA로만 표현.

---

## 4. 호버 팝업(지표 설명) 데이터 구조

원본 문서(v2)에 정의된 JSON 블록을 그대로 프로토타입의 데이터 소스로 사용한다. 화면당 1개의 JSON 그룹을 매핑한다.

| 화면 | JSON 키 |
|---|---|
| 오늘/이번 주/이번 달 (공통 지표 풀) | `track1_indicators` |
| 누가 오는가 | `track2_group1_indicators` |
| 왜·언제 오는가 | `track2_group2_indicators` |
| 돈이 되는가 | `track2_group3_indicators` |

각 지표 객체는 공통 스키마를 따른다:

```json
{
  "지표명": "string",
  "정의": "string",
  "원천데이터_및_산식": "string",
  "제공목적": "string",
  "차트형태": "string"
}
```

- 차트에 마우스를 올리면(모바일은 탭) 해당 카드의 `지표명`~`차트형태` 4개 필드가 툴팁/바텀시트 형태로 노출.
- 이 팝업은 **미팅 참석자(데이터서비스팀/서비스개발팀)가 화면만 보고도 "이 차트를 만들려면 어떤 원천데이터·산식이 필요한지" 바로 이해**하도록 하는 것이 목적 — 즉 프로토타입의 핵심 기능.

---

## 5. 작업 분해 (WBS)

| # | 작업 | 내용 | 산출물 |
|---|---|---|---|
| 1 | 더미데이터 & 지표 JSON 정리 | v2 문서의 JSON 블록을 화면별 파일로 분리, 차트 렌더링용 더미 수치 생성(요일별 누적/평균 규칙 포함) | `data/*.json` |
| 2 | 공통 컴포넌트 설계 | 탭 네비게이션, KPI 2x2 카드, 차트 래퍼(막대/파이/표), 호버 팝업 컴포넌트 | 공통 컴포넌트 |
| 3 | Track1 화면 3종 구현 | 오늘/이번 주/이번 달 탭, 화면별 카드 순서·개수 반영 | 3 화면 |
| 4 | Track2 화면 3종 구현 | 3개 그룹 세로 스크롤, 탭 없음 | 3 화면 |
| 5 | 호버 인터랙션 연결 | 각 차트 → 해당 지표 JSON 팝업 매핑 | 인터랙션 완료 |
| 6 | 모바일 스타일 적용 | 폭 380px, 패딩 16px, 카드간격 12~14px, 색상 시스템(#1d1d1f/#2f7dfb/#1a9850/#e63946) 반영 | 스타일 적용 |
| 7 | 내부 검토 및 수정 | 본인 검토 후 미팅 자료로 최종 확정 | 최종 프로토타입 |

---

## 6. 미팅 대비 별도 확인/논의 아젠다

프로토타입과 별개로, 미팅 자리에서 데이터서비스팀·서비스개발팀에 **직접 확인이 필요한 오픈이슈** (v2 문서 6장 발췌):

- [ ] HPC 상품단위 데이터 존재 여부/스키마 (선호 상품·카테고리 지표 전제조건)
- [ ] HPC 회원 마스터 인구통계 필드 존재 여부
- [ ] 오더 결제수단 필드의 포인트결제 여부/회원ID 매칭 가능 여부
- [ ] 기존 POS 기반 G-CRM ↔ 신규 포인트 기반 G-CRM 통합 방식
- [ ] 매장 가입 상태(오더만/오더+HPC)별 Track2 조건부 노출 로직, 미가입 매장 대체 화면
- [ ] 세그먼트 타겟 쿠폰 연결: 세그먼트별 회원ID 리스트 추출 가능 여부 (쿠폰팀 별도 확인)
- [ ] 시간대 구간 정의 확정 (00~23시 전체 / 영업시간만 / 1시간·30분 단위)
- [ ] Track1(실시간)·Track2(배치) 각각의 데이터 집계 주체 — 오더 API vs DW

이 항목들은 프로토타입 자체에는 영향 없이 진행 가능하나, **화면이 실제로 구현 가능한지 여부와 직결**되므로 미팅에서 우선 확인 대상으로 안내한다.

---

## 7. 일정 (안)

| 일자 | 작업 |
|---|---|
| D0 (오늘) | 더미데이터/JSON 정리, 공통 컴포넌트 설계 |
| D+1 | Track1 3화면 구현 |
| D+2 | Track2 3화면 구현, 호버 팝업 연결 |
| D+3 | 스타일 적용 및 내부 검토, 미팅 자료 최종화 |

> 미팅 일정에 맞춰 위 일정은 조정 가능.

---

## 8. 리스크 / 제약사항

- 본 프로토타입은 **더미데이터 기반 시각 검증용**이며, 실제 데이터 스키마 확인 전까지는 화면상 표현이 실제 구현과 다를 수 있음(특히 Track2의 오픈이슈 지표들).
- "전체 매출" 표현은 v2 문서 제약상 어떤 화면·문구에도 사용하지 않음 (POS 미연동으로 산출 불가).
- 오더 매출과 HPC 매출은 절대 합산하지 않는다는 원칙을 프로토타입 카드 문구/디자인에도 동일하게 반영.
